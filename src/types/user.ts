export type User = {
    id: string
    name?: string
    avatar: string
    username: string
}

export type AuthUser = {
    id: string
    name?: string
    avatar: string
    username: string
}

export type AuthTokens = {
    ata: string
    atr: string
}
